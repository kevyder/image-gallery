%h1 All images
-@images.each do |image|
    %li
        %h3=image.title
        %img{src: image.file, width: '30%'}
%h2 Upload a new image
%form(action='/images' method='post' enctype='multipart/form-data')
    %p
        %input(type='file' name='image[file]' id='file')
    %p
        %input(type='text' name='image[title]' class='form-control' placeholder='Enter the title')
    %p
        %button(class='btn btn-primary') Upload!